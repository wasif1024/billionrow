<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="198" onload="init(evt)" viewBox="0 0 1200 198" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="198" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="181.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="181.00"> </text><svg id="frames" x="10" width="1180" total_samples="26053"><g><title>dyld4::prepare(dyld4::APIs&amp;, mach_o::Header const*) (4 samples, 0.02%)</title><rect x="0.0038%" y="117" width="0.0154%" height="15" fill="rgb(227,0,7)" fg:x="1" fg:w="4"/><text x="0.2538%" y="127.50"></text></g><g><title>DYLD-STUB$$memcmp (171 samples, 0.66%)</title><rect x="0.0192%" y="37" width="0.6564%" height="15" fill="rgb(217,0,24)" fg:x="5" fg:w="171"/><text x="0.2692%" y="47.50"></text></g><g><title>&lt;[A] as core::slice::cmp::SlicePartialEq&lt;B&gt;&gt;::equal (1,370 samples, 5.26%)</title><rect x="0.0192%" y="53" width="5.2585%" height="15" fill="rgb(221,193,54)" fg:x="5" fg:w="1370"/><text x="0.2692%" y="63.50">&lt;[A] a..</text></g><g><title>_platform_memcmp (1,199 samples, 4.60%)</title><rect x="0.6755%" y="37" width="4.6022%" height="15" fill="rgb(248,212,6)" fg:x="176" fg:w="1199"/><text x="0.9255%" y="47.50">_plat..</text></g><g><title>&lt;core::hash::sip::Hasher&lt;S&gt; as core::hash::Hasher&gt;::finish (310 samples, 1.19%)</title><rect x="5.2777%" y="53" width="1.1899%" height="15" fill="rgb(208,68,35)" fg:x="1375" fg:w="310"/><text x="5.5277%" y="63.50"></text></g><g><title>&lt;core::hash::sip::Sip13Rounds as core::hash::sip::Sip&gt;::c_rounds (100 samples, 0.38%)</title><rect x="6.4676%" y="53" width="0.3838%" height="15" fill="rgb(232,128,0)" fg:x="1685" fg:w="100"/><text x="6.7176%" y="63.50"></text></g><g><title>&lt;core::hash::sip::Sip13Rounds as core::hash::sip::Sip&gt;::d_rounds (324 samples, 1.24%)</title><rect x="6.8514%" y="53" width="1.2436%" height="15" fill="rgb(207,160,47)" fg:x="1785" fg:w="324"/><text x="7.1014%" y="63.50"></text></g><g><title>&lt;core::slice::iter::Iter&lt;T&gt; as core::iter::traits::double_ended::DoubleEndedIterator&gt;::next_back (1,044 samples, 4.01%)</title><rect x="8.0950%" y="53" width="4.0072%" height="15" fill="rgb(228,23,34)" fg:x="2109" fg:w="1044"/><text x="8.3450%" y="63.50">&lt;cor..</text></g><g><title>&lt;core::slice::iter::Iter&lt;T&gt; as core::iter::traits::iterator::Iterator&gt;::next (213 samples, 0.82%)</title><rect x="12.1023%" y="53" width="0.8176%" height="15" fill="rgb(218,30,26)" fg:x="3153" fg:w="213"/><text x="12.3523%" y="63.50"></text></g><g><title>&lt;core::slice::iter::Iter&lt;T&gt; as core::iter::traits::iterator::Iterator&gt;::position (5,791 samples, 22.23%)</title><rect x="12.9198%" y="53" width="22.2278%" height="15" fill="rgb(220,122,19)" fg:x="3366" fg:w="5791"/><text x="13.1698%" y="63.50">&lt;core::slice::iter::Iter&lt;T&gt; as core..</text></g><g><title>&lt;core::slice::iter::Iter&lt;T&gt; as core::iter::traits::iterator::Iterator&gt;::rposition (993 samples, 3.81%)</title><rect x="35.1476%" y="53" width="3.8115%" height="15" fill="rgb(250,228,42)" fg:x="9157" fg:w="993"/><text x="35.3976%" y="63.50">&lt;cor..</text></g><g><title>&lt;core::slice::iter::Split&lt;T,P&gt; as core::iter::traits::iterator::Iterator&gt;::next (6,197 samples, 23.79%)</title><rect x="38.9590%" y="53" width="23.7861%" height="15" fill="rgb(240,193,28)" fg:x="10150" fg:w="6197"/><text x="39.2090%" y="63.50">&lt;core::slice::iter::Split&lt;T,P&gt; as core..</text></g><g><title>&lt;std::hash::random::RandomState as core::hash::BuildHasher&gt;::build_hasher (191 samples, 0.73%)</title><rect x="62.7452%" y="53" width="0.7331%" height="15" fill="rgb(216,20,37)" fg:x="16347" fg:w="191"/><text x="62.9952%" y="63.50"></text></g><g><title>alloc::collections::btree::node::NodeRef&lt;alloc::collections::btree::node::marker::Mut,K,V,alloc::collections::btree::node::marker::Internal&gt;::edge_area_mut (4 samples, 0.02%)</title><rect x="63.4783%" y="53" width="0.0154%" height="15" fill="rgb(206,188,39)" fg:x="16538" fg:w="4"/><text x="63.7283%" y="63.50"></text></g><g><title>billionrow::main (1,407 samples, 5.40%)</title><rect x="63.4936%" y="53" width="5.4005%" height="15" fill="rgb(217,207,13)" fg:x="16542" fg:w="1407"/><text x="63.7436%" y="63.50">billion..</text></g><g><title>billionrow::parse_temperature (997 samples, 3.83%)</title><rect x="68.8942%" y="53" width="3.8268%" height="15" fill="rgb(231,73,38)" fg:x="17949" fg:w="997"/><text x="69.1442%" y="63.50">bill..</text></g><g><title>core::core_arch::arm_shared::neon::generated::vreinterpret_u64_u8 (877 samples, 3.37%)</title><rect x="72.7210%" y="53" width="3.3662%" height="15" fill="rgb(225,20,46)" fg:x="18946" fg:w="877"/><text x="72.9710%" y="63.50">cor..</text></g><g><title>&lt;core::hash::sip::Hasher&lt;S&gt; as core::hash::Hasher&gt;::write (1,282 samples, 4.92%)</title><rect x="76.0872%" y="37" width="4.9207%" height="15" fill="rgb(210,31,41)" fg:x="19823" fg:w="1282"/><text x="76.3372%" y="47.50">&lt;core:..</text></g><g><title>&lt;core::hash::sip::Sip13Rounds as core::hash::sip::Sip&gt;::c_rounds (129 samples, 0.50%)</title><rect x="81.0079%" y="37" width="0.4951%" height="15" fill="rgb(221,200,47)" fg:x="21105" fg:w="129"/><text x="81.2579%" y="47.50"></text></g><g><title>core::hash::sip::u8to64_le (234 samples, 0.90%)</title><rect x="81.5031%" y="37" width="0.8982%" height="15" fill="rgb(226,26,5)" fg:x="21234" fg:w="234"/><text x="81.7531%" y="47.50"></text></g><g><title>core::hash::Hasher::write_usize (1,650 samples, 6.33%)</title><rect x="76.0872%" y="53" width="6.3332%" height="15" fill="rgb(249,33,26)" fg:x="19823" fg:w="1650"/><text x="76.3372%" y="63.50">core::ha..</text></g><g><title>core::ptr::copy_nonoverlapping (5 samples, 0.02%)</title><rect x="82.4013%" y="37" width="0.0192%" height="15" fill="rgb(235,183,28)" fg:x="21468" fg:w="5"/><text x="82.6513%" y="47.50"></text></g><g><title>&lt;core::hash::sip::Hasher&lt;S&gt; as core::hash::Hasher&gt;::write (1,057 samples, 4.06%)</title><rect x="82.6277%" y="37" width="4.0571%" height="15" fill="rgb(221,5,38)" fg:x="21527" fg:w="1057"/><text x="82.8777%" y="47.50">&lt;cor..</text></g><g><title>&lt;core::hash::sip::Sip13Rounds as core::hash::sip::Sip&gt;::c_rounds (74 samples, 0.28%)</title><rect x="86.6848%" y="37" width="0.2840%" height="15" fill="rgb(247,18,42)" fg:x="22584" fg:w="74"/><text x="86.9348%" y="47.50"></text></g><g><title>core::hash::sip::u8to64_le (574 samples, 2.20%)</title><rect x="86.9689%" y="37" width="2.2032%" height="15" fill="rgb(241,131,45)" fg:x="22658" fg:w="574"/><text x="87.2189%" y="47.50">c..</text></g><g><title>core::num::_&lt;impl u64&gt;::rotate_left (3 samples, 0.01%)</title><rect x="89.1721%" y="37" width="0.0115%" height="15" fill="rgb(249,31,29)" fg:x="23232" fg:w="3"/><text x="89.4221%" y="47.50"></text></g><g><title>core::ptr::const_ptr::_&lt;impl *const T&gt;::add (20 samples, 0.08%)</title><rect x="89.1836%" y="37" width="0.0768%" height="15" fill="rgb(225,111,53)" fg:x="23235" fg:w="20"/><text x="89.4336%" y="47.50"></text></g><g><title>core::hash::impls::_&lt;impl core::hash::Hash for u8&gt;::hash_slice (2,146 samples, 8.24%)</title><rect x="82.4205%" y="53" width="8.2371%" height="15" fill="rgb(238,160,17)" fg:x="21473" fg:w="2146"/><text x="82.6705%" y="63.50">core::hash:..</text></g><g><title>core::ptr::copy_nonoverlapping (364 samples, 1.40%)</title><rect x="89.2604%" y="37" width="1.3972%" height="15" fill="rgb(214,148,48)" fg:x="23255" fg:w="364"/><text x="89.5104%" y="47.50"></text></g><g><title>core::intrinsics::likely (1,325 samples, 5.09%)</title><rect x="90.6575%" y="53" width="5.0858%" height="15" fill="rgb(232,36,49)" fg:x="23619" fg:w="1325"/><text x="90.9075%" y="63.50">core::..</text></g><g><title>core::num::_&lt;impl u64&gt;::rotate_left (103 samples, 0.40%)</title><rect x="95.7433%" y="53" width="0.3953%" height="15" fill="rgb(209,103,24)" fg:x="24944" fg:w="103"/><text x="95.9933%" y="63.50"></text></g><g><title>core::num::_&lt;impl u64&gt;::wrapping_add (205 samples, 0.79%)</title><rect x="96.1386%" y="53" width="0.7869%" height="15" fill="rgb(229,88,8)" fg:x="25047" fg:w="205"/><text x="96.3886%" y="63.50"></text></g><g><title>core::num::_&lt;impl usize&gt;::to_ne_bytes (39 samples, 0.15%)</title><rect x="96.9255%" y="53" width="0.1497%" height="15" fill="rgb(213,181,19)" fg:x="25252" fg:w="39"/><text x="97.1755%" y="63.50"></text></g><g><title>core::num::nonzero::NonZero&lt;T&gt;::get (146 samples, 0.56%)</title><rect x="97.0752%" y="53" width="0.5604%" height="15" fill="rgb(254,191,54)" fg:x="25291" fg:w="146"/><text x="97.3252%" y="63.50"></text></g><g><title>core::ptr::copy_nonoverlapping (7 samples, 0.03%)</title><rect x="97.6356%" y="53" width="0.0269%" height="15" fill="rgb(241,83,37)" fg:x="25437" fg:w="7"/><text x="97.8856%" y="63.50"></text></g><g><title>core::ptr::mut_ptr::_&lt;impl *mut T&gt;::sub (10 samples, 0.04%)</title><rect x="97.6625%" y="53" width="0.0384%" height="15" fill="rgb(233,36,39)" fg:x="25444" fg:w="10"/><text x="97.9125%" y="63.50"></text></g><g><title>hashbrown::control::bitmask::BitMask::lowest_set_bit (239 samples, 0.92%)</title><rect x="97.7047%" y="53" width="0.9174%" height="15" fill="rgb(226,3,54)" fg:x="25455" fg:w="239"/><text x="97.9547%" y="63.50"></text></g><g><title>hashbrown::control::tag::Tag::full (12 samples, 0.05%)</title><rect x="98.6220%" y="53" width="0.0461%" height="15" fill="rgb(245,192,40)" fg:x="25694" fg:w="12"/><text x="98.8720%" y="63.50"></text></g><g><title>hashbrown::map::equivalent_key::_{{closure}} (284 samples, 1.09%)</title><rect x="98.6681%" y="53" width="1.0901%" height="15" fill="rgb(238,167,29)" fg:x="25706" fg:w="284"/><text x="98.9181%" y="63.50"></text></g><g><title>hashbrown::raw::RawTable&lt;T,A&gt;::len (58 samples, 0.22%)</title><rect x="99.7659%" y="53" width="0.2226%" height="15" fill="rgb(232,182,51)" fg:x="25992" fg:w="58"/><text x="100.0159%" y="63.50"></text></g><g><title>all (26,053 samples, 100%)</title><rect x="0.0000%" y="149" width="100.0000%" height="15" fill="rgb(231,60,39)" fg:x="0" fg:w="26053"/><text x="0.2500%" y="159.50"></text></g><g><title>start (26,052 samples, 100.00%)</title><rect x="0.0038%" y="133" width="99.9962%" height="15" fill="rgb(208,69,12)" fg:x="1" fg:w="26052"/><text x="0.2538%" y="143.50">start</text></g><g><title>main (26,048 samples, 99.98%)</title><rect x="0.0192%" y="117" width="99.9808%" height="15" fill="rgb(235,93,37)" fg:x="5" fg:w="26048"/><text x="0.2692%" y="127.50">main</text></g><g><title>core::ops::function::impls::_&lt;impl core::ops::function::FnOnce&lt;A&gt; for &amp;F&gt;::call_once (26,048 samples, 99.98%)</title><rect x="0.0192%" y="101" width="99.9808%" height="15" fill="rgb(213,116,39)" fg:x="5" fg:w="26048"/><text x="0.2692%" y="111.50">core::ops::function::impls::_&lt;impl core::ops::function::FnOnce&lt;A&gt; for &amp;F&gt;::call_once</text></g><g><title>std::rt::lang_start::_{{closure}} (26,048 samples, 99.98%)</title><rect x="0.0192%" y="85" width="99.9808%" height="15" fill="rgb(222,207,29)" fg:x="5" fg:w="26048"/><text x="0.2692%" y="95.50">std::rt::lang_start::_{{closure}}</text></g><g><title>core::ops::function::FnOnce::call_once (26,048 samples, 99.98%)</title><rect x="0.0192%" y="69" width="99.9808%" height="15" fill="rgb(206,96,30)" fg:x="5" fg:w="26048"/><text x="0.2692%" y="79.50">core::ops::function::FnOnce::call_once</text></g><g><title>hashbrown::raw::RawTableInner::probe_seq (3 samples, 0.01%)</title><rect x="99.9885%" y="53" width="0.0115%" height="15" fill="rgb(218,138,4)" fg:x="26050" fg:w="3"/><text x="100.2385%" y="63.50"></text></g></svg></svg>